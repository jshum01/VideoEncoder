package Video9;

public class ImageBreakDown {
	public ImageBreakDown(int myHeight, int myWidth, boolean alpha, int mypixels[]){
		height = myHeight; width = myWidth; hasAlpha = alpha; pixels = mypixels;
	}
	
	public int height, width, pixels[];
	public boolean hasAlpha;
}

